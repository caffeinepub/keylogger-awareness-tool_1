// This file is intentionally minimal as the backend has no methods.
// All state management is handled client-side via useSimulationState.

export function useQueries() {
  // No backend queries needed for this fully client-side simulation
  return {};
}
